version: '3'
services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      - MONGO_URI=${MONGO_URI}
      - PORT=5000
    depends_on:
      - frontend

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"

volumes:
  mongodb_data: