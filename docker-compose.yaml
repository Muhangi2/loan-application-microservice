version: '3'
services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env  # This line tells Docker to load environment variables from the .env file

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
